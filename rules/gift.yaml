# @import data from '../../data/gift.json'
# Comprehensive test using imported data for autocompletion

- id: general-gift
  on: gift
  do:
    mode: SEQUENCE
    actions:
      - type: log
        params:
          message: ${data.nickname} envio ${data.giftName}
      - type: TTS
        params:
          message: ${data.nickname} envio ${data.giftName}

- id: Rose-gift
  on: gift
  if:
    field: ${data.gift.gift_id}
    operator: EQ
    value: 5655
  do:
    mode: SEQUENCE
    actions:
      - type: log
        params:
          message: ${data.nickname} envio ${data.giftName}
      # TODO: Implement specific actions for rose gift